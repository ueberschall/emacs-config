;; Installiere Auctex, wenn nicht schon geschehen.
;; (install-necessary-packages '(auctex))

;; Das Feature 'auctex' wird vom package nicht provided. Deshalb ist es
;; hier auskommentiert, da use-package nur einen Fehler werfen würde.
;(use-package auctex)

(provide 'setup-tex-packages)
